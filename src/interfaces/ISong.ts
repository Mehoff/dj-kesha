import { Message } from "discord.js";

export interface ISong {
  //   name: string;
  //   description: string;
  //   aliases: string[] | null;
  //   run: (message: Message, args: string[]) => Promise<void>;
}
